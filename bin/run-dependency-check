#!/bin/sh

require_dep() {
    dep=$1
    which -s $dep
    if [ $? -eq 0 ]; then
        echo found: $dep
    else
        echo $dep is required, but not installed
    fi
}

require_dep ruby
require_dep bundle
require_dep node
require_dep npm
require_dep nginx
require_dep doas
